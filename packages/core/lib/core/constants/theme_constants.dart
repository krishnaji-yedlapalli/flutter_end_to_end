abstract class ThemeConstants {
  // Base width (iPhone X)
  static const double baseWidth = 375.0;

  // Breakpoints
  static const double mobileBreakpoint = 375.0;
  static const double tabletBreakpoint = 768.0;
  static const double desktopBreakpoint = 1024.0;

  // Scale factors for different screen sizes
  static const double mobileScaleFactor = 1.0;
  static const double tabletScaleFactor = 1.1;
  static const double desktopScaleFactor = 1.2;

  // Font size limits
  static const double minScaleFactor = 0.8;
  static const double maxScaleFactor = 1.15; // Reduced from 1.4
}
