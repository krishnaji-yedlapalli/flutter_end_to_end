
import 'package:flutter/material.dart';

class LocalAuthentication extends StatelessWidget {
  const LocalAuthentication({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
