name: scrolling
description: Scrolling feature showcase module.
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  go_router:
  app_core:
    path: ../../packages/core
flutter:
  uses-material-design: true
